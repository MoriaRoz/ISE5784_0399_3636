package geometries;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

/**
 * Unit test class for {@link Geometry}.
 */
class GeometryTest {

    /**
     * Test method for {@link Geometry#getNormal(primitives.Point)}.
     * This method will test the getNormal method of the Geometry class
     * to ensure it returns the correct normal vector at various points
     * on the geometry's surface.
     */
    @Test
    void testGetNormal() {
        // Implement test cases for the getNormal method here
    }
}
